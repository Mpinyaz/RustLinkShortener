# Link Shortener (Rust Implementation)

This is a simple link shortener using the following tools:

- Axum (as the web server)
- Sqlx (fully-fledged ORM)
- Prometheus (for metrics)
- Tracing and Logging
- Docker Compose (Postgres)
